package com.Lyambda.car;
/*
public class MainCar {
    private int getCarsNumber(Car[] cars , Searchable s) {
        int result = 0;
        for (Car car : cars) {
            if (s.test(car)) {
                result ++;
            }
        }
        return result;
    }
}
//
carDemo.getCarsNumber(cars, new CompactCarSearch());
        carDemo.getCarsNumber(cars, new ExpensiveCarSearch());
//

 */